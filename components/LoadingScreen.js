import styles from "./LoadingScreen.module.css";

const LoadingScreen = () => {
  return (
    <div className={styles.wrapper}>
      <h1>Loading data...</h1>
    </div>
  );
};

export default LoadingScreen;
